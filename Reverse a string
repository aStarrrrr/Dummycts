using System;
class Program
{
    static void Main()
    {
        string name = Convert.ToString(Console.ReadLine());
        char[] name_array = name.ToCharArray();
        Array.Reverse(name_array);
        Console.WriteLine(name_array);
    }
}
